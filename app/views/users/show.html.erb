<div class="contents row">
    <%= image_tag @user.userimage, class: "user_icon" %>

    <% if current_user.id == @user.id %>
      <div>
        <%= link_to "プロフィール編集", edit_user_path(current_user)%>
      </div>
    <% end %>
</div>

<div class="contents row">
  <%= render partial: "events/event", collection: @events %>
</div>