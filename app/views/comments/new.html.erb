<%= form_for [@event, @comment] do |f| %>
  <div>
    <%= f.text_area :comment, placeholder: 'コメント欄', style: 'width: 100%;height: 100px;' %>
  </div>
  <div>
    <%= button_tag type: "submit", class: "btn btn--block" do %>
      投稿する<i class="icon-arrow-right"></i>
    <% end %>
  </div>
<% end %>

<a href="/">投稿一覧へ戻る</a>